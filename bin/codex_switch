#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
CODEXPM="$SCRIPT_DIR/codexpm"

case "${1:-next}" in
  next|toggle|"") exec "$CODEXPM" next ;;
  status|list) exec "$CODEXPM" list ;;
  *) exec "$CODEXPM" use "$1" ;;
esac
